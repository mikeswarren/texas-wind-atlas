/**
 * Runtime Mapbox token.
 *
 * This file is served as-is and is read before the app bundle, so the deployed
 * site's token can be changed by editing this one line on the server -- no
 * rebuild, no redeploy. Leave it empty and the app shows its setup screen.
 *
 * A Mapbox public token ("pk....") is necessarily visible to the browser. The
 * protection is a URL restriction on the token itself, set in the Mapbox
 * account page -- see README.md.
 */
window.MAPBOX_TOKEN = ''
