# Copy to .env and paste your Mapbox public token.
# Optional: the deployed site can instead set window.MAPBOX_TOKEN in public/config.js,
# which is editable on the server without a rebuild.
VITE_MAPBOX_TOKEN=pk.your_public_token_here
